# 执行指南

## 项目概览

将 YOLO 检测/分类模型的 `best.pt` 转换为 Android 可部署的 `.tflite` 格式。

```
输入:  best.pt          (PyTorch / Ultralytics)
中间:  model.onnx       (ONNX)
       saved_model.pb   (TensorFlow SavedModel)
输出:  model.tflite     (TensorFlow Lite, 含 Flex 算子)
```

---

## 1. 环境准备

### 1.1 首次创建环境

```batch
setup_env.bat
```

创建 `pt2tflite` conda 环境，安装 PyTorch + TensorFlow 2.16 + ultralytics + onnx2tf。

### 1.2 如果 pip install 报"拒绝访问"

Anaconda 管理员安装后 env 目录只读，需授权：

```batch
# 以管理员身份运行（Win + X → 终端(管理员)）
icacls <conda_env_path> /grant "%USERNAME%:(OI)(CI)F" /T /Q
```

将 `<conda_env_path>` 替换为实际路径，例如 `D:\Anaconda3\envs\pt2tflite`。

### 1.3 激活环境

```batch
conda activate pt2tflite
```

或直接指定 Python 路径：

```batch
<python_path> scripts/convert_fall.py --source best.pt
```

---

## 2. 执行转换

### 2.1 最简用法

```batch
cd <project_root>
conda activate pt2tflite

python scripts/convert_fall.py --source <path_to_pt>
```

自动完成 PT → ONNX → TFLite，输出到 `output/`。

### 2.2 自定义参数

```batch
# 指定输出名、图片尺寸、量化方式
python scripts/convert_fall.py ^
    --source best.pt ^
    --name my_model ^
    --imgsz 640 ^
    --quantize fp16

# 只转 ONNX（不做 TFLite）
python scripts/convert_fall.py --source best.pt --step onnx

# 只做到 TF SavedModel
python scripts/convert_fall.py --source best.pt --step tf

# 不量化（FP32 TFLite）
python scripts/convert_fall.py --source best.pt --quantize none
```

### 2.3 使用通用转换器

```batch
python scripts/convert.py --yolo best.pt --imgsz 640
```

### 2.4 从 WSL 调用

WSL 里通过 `cmd.exe /c` 调用 Windows Python 时，**先切到 C:\** 避免 UNC 路径问题：

```bash
# WSL 中执行
cmd.exe /c "cd /d C:\ && <python_path> <project_root>\scripts\convert_fall.py --source <path_to_pt>"
```

---

## 3. 输出产物

转换完成后，`output/` 目录下会生成：

| 文件 | 说明 |
|------|------|
| `xxx.onnx` | ONNX 格式，可用 Netron 可视化 |
| `xxx_fp16.tflite` | TFLite FP16 量化（默认，推荐） |
| `xxx.tflite` | FP32（`--quantize=none` 时） |

命名规则：`{run_name}_{quantize_suffix}.{ext}`。

中间文件（TF SavedModel）保存在 `cache/` 目录，二次转换同模型时可复用。

---

## 4. 验证模型

### 4.1 结构验证

```batch
python -c "
import tensorflow as tf
interp = tf.lite.Interpreter(model_path='output/xxx.tflite')
interp.allocate_tensors()
for d in interp.get_input_details():
    print('Input:', d['name'], d['shape'], d['dtype'])
for d in interp.get_output_details():
    print('Output:', d['name'], d['shape'], d['dtype'])
"
```

### 4.2 推理测试

```batch
python -c "
import tensorflow as tf, numpy as np
interp = tf.lite.Interpreter(model_path='output/xxx.tflite')
interp.allocate_tensors()
inp = interp.get_input_details()[0]
out = interp.get_output_details()[0]
interp.set_tensor(inp['index'], np.zeros((1, *inp['shape'][1:3], 3), dtype=np.float32))
interp.invoke()
result = interp.get_tensor(out['index'])
print('Inference OK, output shape:', result.shape)
"
```

---

## 5. Android 部署

### 5.1 添加 Flex 算子依赖

当前 TFLite 模型包含 `Select TF ops`（如 `FlexSplitV`），Android 上必须添加：

```gradle
// app/build.gradle
dependencies {
    implementation 'org.tensorflow:tensorflow-lite:2.16.1'
    implementation 'org.tensorflow:tensorflow-lite-select-tf-ops:2.16.1'
    implementation 'org.tensorflow:tensorflow-lite-gpu:2.16.1'   // GPU 加速（可选）
}
```

### 5.2 Interpreter 初始化

```kotlin
val tfliteOptions = Interpreter.Options().apply {
    addDelegate(GpuDelegate())       // GPU 加速
    addDelegate(NnApiDelegate())      // NNAPI 回退
}
val interpreter = Interpreter(tfliteModel, tfliteOptions)
```

### 5.3 输出解析

模型输出 YOLO **raw grid 格式** `[1, C, N]`：

| 维度 | 含义 |
|------|------|
| B | batch size（通常为 1） |
| C | 通道：4(bbox) + 1(conf) + 1(cls) + 2(extra) |
| N | 网格点总数（取决于 imgsz 和模型结构） |

需自行实现 **grid decode + NMS** 提取检测框，伪代码：

```kotlin
// 每个网格单元
val cx = (sigmoid(box[0]) * 2 - 0.5 + gridX) * stride
val cy = (sigmoid(box[1]) * 2 - 0.5 + gridY) * stride
val w  = (sigmoid(box[2]) * 2) ^ 2 * anchorW
val h  = (sigmoid(box[3]) * 2) ^ 2 * anchorH
```

> **三级回退策略**：GPU → NNAPI → CPU。竖屏检测时坐标需旋转 90°。

---

## 6. 常见问题

### Q: TFLite 报 `FlexSplitV failed to prepare`

→ 缺少 `tensorflow-lite-select-tf-ops` 依赖。见第 5.1 节。

### Q: 转换时崩于 `'gbk' codec can't decode byte`

→ 中文 Windows 编码问题。本项目已绕过 ultralytics 内置 TFLite 导出，改用手动 onnx2tf 流程。如果仍然遇到，在 cmd 中先执行 `chcp 65001` 再运行。

### Q: `onnx2tf` 很慢或卡住

→ onnx2tf 运行通常需要 1-3 分钟。超过 10 分钟可能是内存不足，尝试减小 `--imgsz` 或换小模型。

### Q: 生成 200+ MB 的巨大 TFLite

→ onnx2tf 可能产生异常输出。删除 `cache/` 目录重试，或检查 ONNX 文件是否包含大量冗余节点。
